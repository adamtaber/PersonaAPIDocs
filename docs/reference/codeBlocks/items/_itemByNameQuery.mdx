import CodeBlock from '@theme/CodeBlock'

<CodeBlock language="jsx" title="QUERY" showLineNumbers>{
`query itemByName($name: Int) {
  itemByName(name: $name) {
    itemId,
    name,
    type,
    effect,
    cost
  }
}
`}</CodeBlock>