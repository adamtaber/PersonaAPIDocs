import CodeBlock from '@theme/CodeBlock'

<CodeBlock language="jsx" title="QUERY" showLineNumbers>{
`query skillByName($name: Int) {
  skillByName(name: $name) {
    skillId,
    name,
    type,
    effect,
    cost
  }
}
`}</CodeBlock>